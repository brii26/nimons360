<?xml version="1.0" encoding="utf-8"?>
<!--
    item grid icon picker , pake di RecyclerView create family
    set isSelected = true di adapter buat highlight yang kepilih, load gambar lewat iv_icon_item
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="56dp"
    android:layout_height="56dp"
    android:background="@drawable/bg_icon_picker_item"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackgroundBorderless">

    <ImageView
        android:id="@+id/iv_icon_item"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="center"
        android:scaleType="centerCrop"
        android:contentDescription="@null"/>

</FrameLayout>
