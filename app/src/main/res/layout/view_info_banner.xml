<?xml version="1.0" encoding="utf-8"?>
<!--
    <include layout="@layout/view_info_banner" android:id="@+id/info_banner"/>
    set teksnya pake binding.infoBanner.tv_banner_text.text = "..."
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_info_banner"
    android:paddingVertical="10dp"
    android:paddingStart="12dp"
    android:paddingEnd="12dp">

    <ImageView
        android:id="@+id/iv_banner_icon"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:src="@drawable/ic_info"
        android:contentDescription="@null"
        app:tint="#2196F3"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <TextView
        android:id="@+id/tv_banner_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:textSize="13sp"
        android:textColor="#1565C0"
        android:lineSpacingMultiplier="1.3"
        app:layout_constraintStart_toEndOf="@id/iv_banner_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
